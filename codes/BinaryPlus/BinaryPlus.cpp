#include <iostream>
#include <string>

using namespace std;

string BinaryPlus(string& s1, string& s2)
{
	string resultStr = "";
	char carryOne = '\0';

	for (int idx1 = s1.length() - 1, idx2 = s2.length() - 1; s1.length() >= 0 && s2.length() >= 0; --idx1, --idx2)
	{
	}
	
	return resultStr;
}

int main()
{

	return 0;
}
